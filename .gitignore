node_modules/
dist/
coverage/
playwright-report/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store

# Real receipt photos used for Task 07 model evaluation — contain real names, addresses, OIBs.
# Only recorded Azure JSON responses (api/src/providers/document-extraction/fixtures/) are committed.
.agents/fixtures/
